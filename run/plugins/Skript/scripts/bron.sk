on join:
    wait 1 tick
    player is op
    set player's gamemode to creative

on player world change:
    player is op
    wait 1 ticks
    set player's gamemode to creative
    make player fly

on respawn:
    wait 1 tick
    player is op
    set player's gamemode to creative


on spawn:
    cancel event if spawn reason is natural
